<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100..900;1,100..900&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Roboto", serif;
      }

      .main-heading {
        font-size: 24px;
        font-weight: bold;
        font-family: "Roboto", serif;
      }

      .input-field {
        padding: 10px 20px;
        border-radius: 5px;
        min-width: 80%;
      }

      .copy-token-button {
        padding: 0px 10px;
        border-radius: 5px;
        cursor: pointer;
      }

      .copy-token-button:active {
        background-color: lightgrey;
      }

      .input-button-div {
        display: flex;
        flex-direction: column;
        gap: 20px;
      }

      .simple-text {
        font-size: 15px;
      }
    </style>
  </head>

  <body>
    <p class="main-heading">Click on the button to copy the token</p>

    <div class="input-button-div">
      <div>
        <input
          id="resetToken"
          class="input-field"
          value="<%=resetToken%>"
          readonly
        />
      </div>

      <div>
        <p class="simple-text">
          After copying token paste that into appropriate place provided in the
          application
        </p>
      </div>
    </div>
  </body>
</html>
